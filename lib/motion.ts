/**
 * Shared motion values. Every landing section reveals with the same curve and
 * duration so the page reads as one surface rather than a stack of widgets.
 */

export const EASE_OUT: [number, number, number, number] = [0.16, 1, 0.3, 1];

export const REVEAL_DURATION = 0.24;

export const REVEAL_DISTANCE = 16;

export const STAGGER = 0.06;
